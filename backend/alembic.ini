# Alembic placeholder — DO NOT RUN YET.
# Migrations are configured and owned by Phase 02 (models + first migration).
# This header exists only so the file is present in the scaffold; the real
# [alembic] section, script_location, and sqlalchemy.url wiring land in Phase 02.
# Reminder: schema is owned by migrations only — never Base.metadata.create_all.
